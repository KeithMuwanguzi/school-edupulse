"use client";

import { CircularsInboxView } from "@/components/domain/communication/CircularsInboxView";

export default function CircularsPage() {
  return (
    <CircularsInboxView
      title="School circulars"
      description="Notices from your school — fees, events, closures, and updates."
    />
  );
}
